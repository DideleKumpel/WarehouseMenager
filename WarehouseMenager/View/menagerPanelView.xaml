<UserControl x:Class="WarehouseMenager.View.menagerPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarehouseMenager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="70*"/>
            <ColumnDefinition Width="30*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="78*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="10">
            <Button Width="100" Height="50" Margin="10" Name="BtnDdd" Command="{Binding AddCommand}" >Add</Button>
            <Button Width="100" Height="50" Margin="10" Name="BtnDelete" Command="{Binding DeleteCommand}" >Delete</Button>
            <Button Width="100" Height="50" Margin="10" Name="BtnRefresh" Command="{Binding DoneCommand}" >Refresh</Button>
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="10" 
              ItemsSource="{Binding Items}"
              SelectedItem="{Binding SelectedItem}"
              CanUserAddRows="False" CanUserDeleteRows="False"
              AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Taks" Binding="{Binding Task}" Width="20*"/>
                <DataGridTextColumn Header="Ramp" Binding="{Binding Ramp}" Width="20*"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="20*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="20*"/>
                <DataGridCheckBoxColumn Header="Status" Binding="{Binding Status}" Width="20*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="10">
            <StackPanel Orientation="Vertical" >
                <Label>Welcome:</Label>
                <Label Content="*username*"/>
            </StackPanel>
            <Button Height="40" Width="70" Margin="50,0,0,0">Log out</Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10">
            <Label>Number of free spaces in warhouse:</Label>
            <Label Content="*nuber*"/>
            <Label Content="Task: "/>
            <ComboBox  Margin="5,0,5,10" />
            <Label Content="Ramp: "/>
            <TextBox  Margin="5,0,5,10" Text="{Binding SelectedItem.Place, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Product: "/>
            <ComboBox  Margin="5,0,5,10" />
            <Label Content="Date: "/>
            <DatePicker Margin="5,0,5,10" SelectedDate="{Binding SelectedItem.Date, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Amount: "/>
            <TextBox  Margin="5,0,5,10" Text="number" />
        </StackPanel>
    </Grid>

</UserControl>
