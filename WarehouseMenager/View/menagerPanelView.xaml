<UserControl x:Class="WarehouseMenager.View.menagerPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarehouseMenager.View"
             mc:Ignorable="d" Height="650" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="70*"/>
            <ColumnDefinition Width="30*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="78*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="10">
            <StackPanel Margin="0,0,20,0">
                <Label FontSize="24">You are current in:</Label>
                <Label FontSize="24">TASK MANAGER</Label>
            </StackPanel>
            <Button Width="100" Height="50" Margin="10" Name="BtnProductMenagerView"  Command="{Binding SwitchPorductMengerView}" >
                <TextBlock Padding="3,3,3,3" TextWrapping="Wrap" TextAlignment="Center" Text="Swich to product menager" />
            </Button>
            <Button Width="100" Height="50" Margin="10" Name="BtnOperatorView"  Command="{Binding SwitchOperatorPanelView}" >
                <TextBlock Padding="3,3,3,3" TextWrapping="Wrap" TextAlignment="Center" Text="Swich to operator panel" />
            </Button>
            <Button Width="100" Height="50" Margin="10" Name="BtnRefresh" Command="{Binding RefreshCommand}" >Refresh</Button>
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="10" 
              ItemsSource="{Binding Tasks}"
              SelectionMode="Extended"
              SelectionUnit="FullRow"
              SelectionChanged="DataGrid_SelectionChanged"
              CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
              AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Taks" Binding="{Binding Type}" Width="6*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Product.Barcode}" Width="20*"/>
                <DataGridTextColumn Header="Ramp" Binding="{Binding Ramp.Name}" Width="6*"/>
                <DataGridTextColumn Header="Shelf" Binding="{Binding Location.Shelf}" Width="6*"/>
                <DataGridTextColumn Header="Row" Binding="{Binding Location.Row}" Width="6*"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Location.Level}" Width="6*"/>
                <DataGridTextColumn Header="Uppload Date" Binding="{Binding UploadDate}" Width="20*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="6*"/>
                <DataGridTextColumn Header="Assigned employee" Binding="{Binding AssingedEmployeeDisplay}" Width="24*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="10">
            <StackPanel Orientation="Vertical" >
                <Label FontSize="24">Welcome:</Label>
                <Label FontSize="24" Width="200">
                    <TextBlock Padding="3,3,3,3" TextWrapping="Wrap" TextAlignment="Center" Text="{Binding UsernameDisplay, UpdateSourceTrigger=PropertyChanged}" />
                </Label>
            </StackPanel>
            <Button Height="60" Width="90" Margin="30,0,0,0" Command="{Binding LogOutCommand}">Log out</Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10">
            <Label>Warehouse fill status:</Label>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <Label Margin="0,0,0,5" Width="300" HorizontalContentAlignment="Center" Content="{Binding SpaceRatioInWarehouseDisplay, UpdateSourceTrigger=PropertyChanged}"/>
                <ProgressBar Height="15" Width="300" Minimum="0" Maximum="100" Value="{Binding FillnesProcentage, UpdateSourceTrigger=PropertyChanged}"></ProgressBar>
            </StackPanel>
            <Label Content="Task: "/>
            <StackPanel Orientation="Horizontal">
                <RadioButton Margin="15,0,15,0" IsChecked="{Binding LoadBtn}" Tag="LoadRBtn" >Load</RadioButton>
                <RadioButton Margin="15,0,15,0" IsChecked="{Binding UnloadBtn}" Tag="UnLoadRBtn" >Unload</RadioButton>
            </StackPanel>
            <Label Content="Ramp: "/>
            <ComboBox  Name="RampIdSelectorComboBox" Margin="5,0,5,10" SelectedItem="{Binding SelectedRamp, Mode=TwoWay}" DisplayMemberPath="Name" ItemsSource="{Binding Ramps, Mode=TwoWay}"/>
            <Label Content="Product: "/>
            <ComboBox  Margin="5,0,5,10" Name="ProductIdSelectorComboBox" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" DisplayMemberPath="Barcode" ItemsSource="{Binding Products, Mode=TwoWay}"/>
            <Label Content="Amount: "/>
            <TextBox  Margin="5,0,5,10" PreviewTextInput="IsNumber" Text="{Binding AmountInput}" MaxLength="3"/>
            <StackPanel Orientation="Horizontal">
                <Button Width="100" Height="50" Margin="10" Name="BtnDdd" Command="{Binding AddCommand}" >Add</Button>
                <Button Width="100" Height="50" Margin="10" Name="BtnDelete" Command="{Binding DeleteCommand}" >Delete</Button>
            </StackPanel>
        </StackPanel>
    </Grid>

</UserControl>
