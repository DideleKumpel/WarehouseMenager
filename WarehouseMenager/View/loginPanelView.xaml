<UserControl x:Class="WarehouseMenager.View.loginPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarehouseMenager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Orientation="Vertical">
            <Label Width="113" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10">Log in</Label>
            <Label Width="130" FontSize="12">Username</Label>
            <TextBox Width="130" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label Width="130" FontSize="12" >Password</Label>
            <PasswordBox Width="130" Margin="0,0,0,20" PasswordChanged="PasswordBox_PasswordChanged"></PasswordBox>
            <Button Width="75" Command="{Binding LoginCommand}">Log in</Button>
        </StackPanel>
    </Grid>
</UserControl>
