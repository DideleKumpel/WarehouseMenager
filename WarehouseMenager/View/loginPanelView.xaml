<UserControl x:Class="WarehouseMenager.View.loginPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarehouseMenager.View"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1200">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Height="350">
            <Label Width="250" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,10">Log in</Label>
            <Label Width="250" FontSize="24">Username</Label>
            <TextBox Width="250" FontSize="24" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label Width="250" FontSize="24" >Password</Label>
            <PasswordBox Width="250" FontSize="24" Margin="0,0,0,20" PasswordChanged="PasswordBox_PasswordChanged"></PasswordBox>
            <Button Width="100" Height="50" Command="{Binding LoginCommand}">Log in</Button>
        </StackPanel>
    </Grid>
</UserControl>
