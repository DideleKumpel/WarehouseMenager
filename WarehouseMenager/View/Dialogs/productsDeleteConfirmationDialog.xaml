<Window x:Class="WarehouseMenager.View.Dialogs.productsDeleteConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarehouseMenager.View.Dialogs"
             mc:Ignorable="d" 
             Title="Confirm Deletion" Height="400" Width="800" WindowStartupLocation="CenterOwner">
    <Grid>
        <TextBlock Text="Are you sure you want to delete the selected products?" FontSize="16" FontWeight="Bold" Margin="10"/>
        <DataGrid ItemsSource="{Binding SelectedProducts}" 
              AutoGenerateColumns="False" 
              Margin="10,40,10,50"
              IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="15*"/>
                <DataGridTextColumn Header="Weight" Binding="{Binding Weight}" Width="8*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="15*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="20*"/>
                <DataGridTemplateColumn Header="Description"  Width="32*" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock Text="{Binding Description}"
                                TextWrapping="Wrap"
                                VerticalAlignment="Top"
                                MaxHeight="150" />
                            </ScrollViewer>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Bottom">
            <Button Content="Yes" Width="80" Margin="5" Click="YesButton_Click"/>
            <Button Content="No" Width="80" Margin="5" Click="NoButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
