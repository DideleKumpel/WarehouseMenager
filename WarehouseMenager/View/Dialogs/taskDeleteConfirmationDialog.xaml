<Window x:Class="WarehouseMenager.View.Dialogs.taskDeleteConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseMenager.View.Dialogs"
        mc:Ignorable="d"
        Title="Confirm Deletion" Height="400" Width="800" WindowStartupLocation="CenterOwner">
    <Grid>
        <TextBlock Text="Are you sure you want to delete the selected tasks?" FontSize="16" FontWeight="Bold" Margin="10"/>
        <DataGrid ItemsSource="{Binding SelectedTasks}" 
                  AutoGenerateColumns="False" 
                  Margin="10,40,10,50"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Taks" Binding="{Binding Type}" Width="6*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Product.Barcode}" Width="20*"/>
                <DataGridTextColumn Header="Ramp" Binding="{Binding Ramp.Name}" Width="6*"/>
                <DataGridTextColumn Header="Shelf" Binding="{Binding Location.Shelf}" Width="6*"/>
                <DataGridTextColumn Header="Row" Binding="{Binding Location.Row}" Width="6*"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Location.Level}" Width="6*"/>
                <DataGridTextColumn Header="Uppload Date" Binding="{Binding UploadDate}" Width="20*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="6*"/>
                <DataGridTextColumn Header="Assigned employee" Binding="{Binding AssingedEmployeeDisplay}" Width="24*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Bottom">
            <Button Content="Yes" Width="80" Margin="5" Click="YesButton_Click"/>
            <Button Content="No" Width="80" Margin="5" Click="NoButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
